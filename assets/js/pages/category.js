$(async function(){ await OLP.loadData(); const data = OLP.data; if (!data) return; const name = OLP.getParam('name'); if (!name) { location.href = '404.html'; return; } $('#categoryTitle').text(name); const list = data.courses.filter(c => c.category === name); const grid = $('#categoryGrid'); if (list.length === 0) { grid.html('<div class="notice">No courses in this category yet.</div>'); return; } list.forEach(c => { grid.append(` <div class="card course-row"> <img src="${c.image}" alt=""> <div> <div class="title"><a href="course.html?id=${encodeURIComponent(c.id)}">${c.title}</a></div> <div class="desc">${c.description}</div> <div class="tags"> <span class="badge">${c.level}</span> <span class="badge">${OLP.fmtPrice(c.price)}</span> <span class="badge">${c.duration_hours}h</span> <span class="badge">${OLP.star(c.rating)}</span> </div> </div> </div> `); }); });