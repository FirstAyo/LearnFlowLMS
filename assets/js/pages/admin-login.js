$(function(){ $('#loginForm').on('submit', function(e){ e.preventDefault(); const u = $('#username').val().trim(); const p = $('#password').val().trim(); if ((u === 'admin') && (p === 'admin123')) { localStorage.setItem('olp_admin_token', 'ok'); location.href = 'admin-dashboard.html'; } else { $('#msg').html('<div class="notice">Invalid credentials. Try admin / admin123</div>'); } }); });